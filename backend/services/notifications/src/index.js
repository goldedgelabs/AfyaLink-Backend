const express = require('express'); const app = express(); app.use(express.json()); app.post('/notifications/email', async (req,res)=>{ const { to, subject, html } = req.body||{}; res.json({ ok:true, to, subject }); }); app.post('/notifications/sms', async (req,res)=>{ res.json({ ok:true }); }); const PORT = process.env.PORT||4012; app.listen(PORT, ()=>console.log('Notifications service on',PORT));